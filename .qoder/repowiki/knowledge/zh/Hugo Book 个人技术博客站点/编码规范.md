- 每个文档章节在对应目录下放置 `_index.md` 作为该节的聚合首页，子页面通过文件名前缀数字排序（如 `021-Codes/`、`40-EmbeededLinux.md`）。
- 所有可交互元素（Mermaid 图、KaTeX 公式、HTML 片段）统一通过 Goldmark 的 `unsafe: true` 渲染，而非外部脚本注入。
- 站点元信息（标题、菜单、主题参数、评论仓库等）集中维护在根级 `config.yaml` 的 `params` 段，避免分散在各页面 front matter。